
<li class="margin_bottom">
    <!-- this needs to be changed as the commenters profile image currently it is the profile image of blog post owner use comment.user.image.url istead of @blog.use.iamge.url-->
    <div class="avatar-image-container"><%= image_tag @blog.user.image.url, :size => "46x46" %>"></div>

    <div class="comment-block">
        <div class="comment-header" kind="m">
            <cite class="user blog-author">
                <b rel="nofollow" href="#"><%= comment.user.name.titleize %></b>
            </cite>
            <!--<span class="icon user blog-author">-->
            <!--</span>-->
            <span class="datetime">
                <i><%= comment.created_at.strftime('%b %d, %Y at %I:%M %p')%></i>
            </span>
            <p class="comment-content"><%= comment.comment_body%></p>
        </div>
    </div>
</li>