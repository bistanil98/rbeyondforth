<div class="bs-example">
    <table class="table">
        <caption><h3>Latest <%= @result.count %> websites checked for validations</h3></caption>
        <thead>
        <tr class="info">
            <th>S.No.</th>
            <th>Site Name</th>
            <th>Validation Errors</th>
            <th>Visit Date & Time</th>
        </tr>
        </thead>
        <tbody>
        <!--variable added for serial no.-->
        <% i=1 %>
        <% @result.each do |d| %>
            <tr class="active">
                <% @webid = d.id %>
                <td><%= i %></td>
                <td><%=link_to d.site_uri, seo_get_error_data_path(:webid => @webid) , remote: true%></td>
                <td><%= d.errnum %></td>
                <td><%= d.created_at %></td>
            </tr>
            <% i=i+1 %>
        <% end %>
        </tbody>
    </table>
</div>
